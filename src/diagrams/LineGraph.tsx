import React, { useEffect, useRef } from "react";
// @ts-ignore
import * as d3 from "d3";

const LineGraph = () => {
    return (
        <div>
            line graph
        </div>
    );
};

export default LineGraph;